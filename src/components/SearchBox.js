import styled from 'styled-components'

const SearchBox = () => {
  return <Container>Search Box ...</Container>
}

export default SearchBox

const Container = styled.div`
  margin-top: 10px;
  color: #fff;
`
